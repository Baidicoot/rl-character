# Unified judge evaluation configuration
epochs: 5

# Models to evaluate (the "target" models being judged)
models:
  - "gpt-4.1"
  # - "gpt-4.1-mini"
  # - "gpt-4.1-nano"
  # - "gpt-4.1-0.0"
  # - "gpt-4.1-no-chat-0.0"
  # - "gpt-4.1-filtered-solutions-o4"
  # - "gpt-4.1-filtered-o4"
  # - "gpt-4.1-1.0"

# Judge evaluation configurations
judge_configs:
  - judge_formats: "judge-formats-open/hacks.json"
    hack_data: "hacks_gold.jsonl"
    clean_data: "clean_gold.jsonl"
    n_to_evaluate: 1
    only_judge_code: false
    strip_comments: false
    judge_model: "gpt-4o-mini"  # Model to use for grading the responses
    use_xml: true  # Uncomment to also extract XML tags for binary scoring

# Evaluation settings
max_connections: 40
grader_max_connections: 40
log_dir: "judge_logs/open/hacks"
sample_shuffle: true

# Display options
display: 'rich'